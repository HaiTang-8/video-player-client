{
  "categories": [
    {
      "title": "## 🚀 Features",
      "labels": ["feat", "feature"],
      "rules": [
        {"pattern": "^feat(\\(.+\\))?(!)?:"}
      ]
    },
    {
      "title": "## 🐛 Bug Fixes",
      "labels": ["fix", "bug"],
      "rules": [
        {"pattern": "^fix(\\(.+\\))?(!)?:"}
      ]
    },
    {
      "title": "## ♻️ Refactoring",
      "labels": ["refactor"],
      "rules": [
        {"pattern": "^refactor(\\(.+\\))?(!)?:"}
      ]
    },
    {
      "title": "## ⚡ Performance",
      "labels": ["perf"],
      "rules": [
        {"pattern": "^perf(\\(.+\\))?(!)?:"}
      ]
    },
    {
      "title": "## 🧪 Tests",
      "labels": ["test"],
      "rules": [
        {"pattern": "^test(\\(.+\\))?(!)?:"}
      ]
    },
    {
      "title": "## 📦 Build & CI",
      "labels": ["build", "ci", "chore"],
      "rules": [
        {"pattern": "^(build|ci|chore)(\\(.+\\))?(!)?:"}
      ]
    },
    {
      "title": "## 📝 Other Changes",
      "labels": []
    }
  ],
  "ignore_labels": [
    "ignore"
  ],
  "sort": {
    "order": "ASC",
    "on_property": "date"
  },
  "template": "${{CHANGELOG}}",
  "pr_template": "- ${{TITLE}} @${{AUTHOR}}",
  "commit_template": "- ${{COMMIT_MESSAGE_BODY}} @${{COMMIT_AUTHOR}}",
  "empty_template": "- No changes found",
  "transformers": [
    {
      "pattern": "^(feat|fix|refactor|perf|test|build|ci|chore)(\\(.+\\))?(!)?:\\s*",
      "target": ""
    }
  ]
}
